[project]
name = "slotsentry"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "asalbaharlouie", email = "asalbaharlouie@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
]

[project.scripts]
slotsentry = "slotsentry:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
